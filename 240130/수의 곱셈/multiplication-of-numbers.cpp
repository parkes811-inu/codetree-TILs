#include <iostream>

using namespace std;

int num[3];
bool cmp[3];
int answer;

int main() {
    // 여기에 코드를 작성해주세요.

    // for(int i = 0; i < 3; i++) {
    //     cin >> num[i];

    //     if(num[i] % 2 != 0) {
    //         cmp[i] = true;
    //     }
    // }

    // for(int i = 0; i < 3; i++) {

    //     if(cmp[i] == true) {

    //     }
    // }
    int a, b, c;

    cin >> a >> b >> c;

    cout << a * b * c;

    return 0;
}